apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-bus.fullname" . }}
  labels:
    {{- include "event-bus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-bus.selectorLabels" . | nindent 4 }}
