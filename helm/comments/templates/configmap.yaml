apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "comments.fullname" . }}-config
  labels:
    {{- include "comments.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.config.NODE_ENV | quote }}
