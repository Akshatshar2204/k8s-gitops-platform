apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "posts.fullname" . }}
  labels:
    {{- include "posts.labels" . | nindent 4 }}

spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}

  selector:
    matchLabels:
      {{- include "posts.selectorLabels" . | nindent 6 }}

  template:
    metadata:
      labels:
        {{- include "posts.selectorLabels" . | nindent 8 }}

    spec:
      containers:
        - name: posts
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP

          envFrom:
            - configMapRef:
                name: {{ include "posts.fullname" . }}-config

            - secretRef:
                name: {{ include "posts.fullname" . }}-secret

          resources:
            {{- toYaml .Values.resources | nindent 12 }}
