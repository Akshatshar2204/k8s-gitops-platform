apiVersion: v1
kind: Service
metadata:
  name: {{ include "moderation.fullname" . }}
  labels:
    {{- include "moderation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moderation.selectorLabels" . | nindent 4 }}
