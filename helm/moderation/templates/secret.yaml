apiVersion: v1
kind: Secret
metadata:
  name: {{ include "moderation.fullname" . }}-secret
  labels:
    {{- include "moderation.labels" . | nindent 4 }}

type: Opaque

stringData:
  APP_SECRET: {{ .Values.secret.appSecret | quote }}
