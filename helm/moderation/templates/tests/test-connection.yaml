apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moderation.fullname" . }}-test-connection"
  labels:
    {{- include "moderation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moderation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
